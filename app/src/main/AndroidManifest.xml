<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bankingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankingApp">
        <activity
            android:name=".SendToCustomerList"
            android:parentActivityName=".CustomerData"
            android:exported="false" />
        <activity
            android:name=".CustomerData"
            android:parentActivityName=".CustomersList"
            android:exported="false" />
        <activity
            android:name=".TransactionHistory"
            android:parentActivityName=".HomeScreen"
            android:exported="false" />
        <activity
            android:name=".CustomersList"
            android:parentActivityName=".HomeScreen"
            android:exported="false" />
        <activity
            android:name=".HomeScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>